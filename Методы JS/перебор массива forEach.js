// Перебор элементов массива

// Метод forEach

let arr = ["Юля", "Ваня", "Вася", "Юра", "Ана", "Таня", "Татьяна"];
ar.forEach((item, index, array) => {
  console.log(`${item} находится на ${index} позиции в  ${array}`);
});

// или
arr.forEach(show);
function show(item) {
  console.log(item);
}
